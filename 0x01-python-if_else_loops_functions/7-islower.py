#!/usr/bin/python3
# Function that checks for lowercase charactors

def islower(charctor):
    """Checks for lowercase charactors."""
    if ord(charactor) >= 97 and ord(charactor) <= 122:
        return True
    else:
        return False
